import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'nosfaltauno',
  webDir: 'out' // <- carpeta generada por next export
};

export default config;
